# MySQL Database Connection
# Replace YOUR_PASSWORD with your actual MySQL password
spring.datasource.url=jdbc:mysql://localhost:3306/gym_management
spring.datasource.username=root
spring.datasource.password=53WR*!eFr1pr7kI!$*=Lw3oFuR?wr4fR
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate Settings
# 'update' means it will auto-update the table structure (great for beginners)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# App runs on port 8080
server.port=8080

# Allow React frontend (running on port 5173) to call our APIs
# This is called CORS configuration
spring.web.cors.allowed-origins=http://localhost:5173